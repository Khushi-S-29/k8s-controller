apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: numericals.maths.mydomain.com
spec:
  group: maths.mydomain.com
  names:
    kind: Numericals
    listKind: NumericalsList
    plural: numericals
    singular: numerical
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Numericals is the Schema for the numericals API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NumericalsSpec defines the desired state of Numericals
            properties:
              expression:
                description: Expression is a math expression of Numericals, which the user wants to solve.
                type: string
            type: object
          status:
            description: NumericalsStatus defines the observed state of Numericals
            properties:
              answer:
                description: Answer is the solution to the expression
                type: string
            required:
            - answer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
